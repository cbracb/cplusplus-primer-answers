// pointers are objects, reference are not

// since pointers are objects, it can bind to different address at its lifetime
// unlike reference

// can't create a reference to a reference since a reference is not an object
// can create a pointer to a pointer since a pointer is an object