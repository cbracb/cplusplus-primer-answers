// basically include the code inside the < and > symbol
#include <iostream>

// the first function that the OS will call
int main(){
    // std::cout -- console output
    // std::endl -- end line 
    std::cout << "Hello, World" << std::endl;
    // returning '0' to 'main' means 'exited successfully' 
    return 0;
}