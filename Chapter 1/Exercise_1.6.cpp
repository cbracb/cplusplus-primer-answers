/**
 *	std::cout << "The sum of " << v1;
 * 		  	  << " and " << v2; 
 * 			  << " is " << v1 + v2;
 * 
 */
/**
 * The codes isn't legal since the statement preceeding the 2nd left shift
 * operator have no ostream to put the data on due to the semicolon following
 * the 'v1' variable
 */